{
  "name": "tekst",
  "displayName": "Tekst",
  "scripts": {
    "format": "prettier --write **/*.{js,css,yaml,toml}"
  },
  "devDependencies": {
    "prettier": "^3.8.3",
    "prettier-plugin-toml": "^2.0.6"
  }
}
