{
  "name": "tekst",
  "displayName": "Tekst",
  "scripts": {
    "format": "prettier --write **/*.{js,css,yaml,toml}"
  },
  "devDependencies": {
    "@taplo/cli": "^0.7.0",
    "prettier": "^3.8.3",
    "prettier-plugin-toml": "^2.0.6"
  }
}
